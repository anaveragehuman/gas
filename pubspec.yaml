name: gastracker

publish_to: 'none'

version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  json_annotation: ^4.0.1

dev_dependencies:
  build_runner:
  json_serializable: ^4.1.4
  test: ^1.17.10
  very_good_analysis:

flutter:
  uses-material-design: true
