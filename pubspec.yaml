name: gastracker

publish_to: 'none'

version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^7.0.1
  freezed_annotation: ^0.14.2
  http: ^0.13.3

dev_dependencies:
  bloc_test: ^8.1.0
  build_runner:
  freezed: ^0.14.2
  json_serializable: ^4.1.4
  test: ^1.17.10
  very_good_analysis:

flutter:
  uses-material-design: true
